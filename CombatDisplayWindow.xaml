<Window x:Class="XMLCharSheets.CombatDisplayWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Combatants" Height="300" Width="300">
    <Grid>
        <ListBox 
                    Name="ActiveCharacters_ListBox"
                    Background="Black"
                    SelectedItem="{Binding Path=SelectedActiveCharacter, Mode=TwoWay}"
                    ItemsSource="{Binding Path=ActiveRoster, UpdateSourceTrigger=PropertyChanged}" 
                    Height="Auto"
                    Focusable="False">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <DockPanel Focusable="False">
                        <TextBlock Text="{Binding Path=Name}" FontSize="24" Foreground="{Binding Path=StatusColor}" HorizontalAlignment="Left" />
                        <TextBlock Margin="16,0,0,0" Text="{Binding Path=StatusesLine}" Foreground="{Binding Path=StatusColor}" HorizontalAlignment="Left" />
                        <TextBlock FontSize="20" Margin="8,0,0,0" Foreground="{Binding Path=Target.StatusColor}"  Text="{Binding Path=Target.Name, StringFormat=Targetting {0:c}!}" 
                                           HorizontalAlignment="Center" />
                    </DockPanel>
                </DataTemplate>
            </ListBox.ItemTemplate>

        </ListBox>

    </Grid>
</Window>
